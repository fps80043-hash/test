# MeowTool Web

Веб-версия MeowTool для работы с Roblox cookies и прокси.

## Функции

- **Proxy Checker** - Проверка прокси на работоспособность
- **Cookie Checker** - Проверка Roblox куки и получение информации об аккаунте
- **Cookie Sorter** - Сортировка и очистка куки от дубликатов
- **Cookie Refresher** - Обновление куки
- **Place Parser** - Парсинг геймпассов и бейджей с плейсов

## Деплой на Railway

### 1. Создай репозиторий на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/meowtool-web.git
git push -u origin main
```

### 2. Подключи к Railway

1. Зайди на [railway.app](https://railway.app)
2. Нажми "New Project" → "Deploy from GitHub repo"
3. Выбери свой репозиторий
4. Railway автоматически определит Dockerfile

### 3. Настрой переменные окружения (опционально)

Никакие переменные не требуются по умолчанию.

### 4. Получи домен

После успешного деплоя Railway выдаст тебе домен.

## Локальный запуск

```bash
# Установи зависимости
pip install -r backend/requirements.txt

# Запусти сервер
python backend/main.py
```

Сервер будет доступен на `http://localhost:8000`

## API Endpoints

- `POST /api/proxy/check` - Проверка прокси
- `POST /api/cookie/check` - Проверка куки
- `POST /api/cookie/refresh` - Обновление куки
- `POST /api/cookie/sort` - Сортировка куки из файла
- `POST /api/place/gamepasses` - Парсинг геймпассов
- `POST /api/place/badges` - Парсинг бейджей
- `GET /api/health` - Проверка статуса

## Лицензия

MIT License - Только добра. 🐾
